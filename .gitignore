.DS_Store
.next
coverage
node_modules
*.log

# Local env files (may contain secrets)
.env.local
.env*.local

# TypeScript incremental build cache
tsconfig.tsbuildinfo

.vercel

# Optional local GT Walsheim Pro (not shipped; Velvet Folio falls back to Inter)
fonts/gt-walsheim/
